<%= render "core/navbar_generic" %>
<div class="banner page-banner d-flex align-items-center" style="height: 30vh; background-image: url('<%= @list.photo.attached? ? cl_image_path(@list.photo.key) : image_path('filmsetback2.jpg') %>')">
  <div class="container">
    <div class="row">
      <div class="col-6 p-3 d-flex align-items-center">
        <div>
          <h1 class="mb-3 banner-h1-m"><%= @list.name %></h1>
          <%= link_to "Add a movie", new_list_bookmark_path(@list), class: "btn btn-core btn-sm" %>
        </div>
      </div>
      <div class="col-6 p-3">
      </div>
    </div>
  </div>
</div>
<div class="mid-container">
  <div class="container p-3">
    <% if @list.bookmarks.any? %>
      <%= render "movies" %>
    <% else %>
      <h2 style="padding: 50px 0 113px 0;">You don't have any movies yet.</h2>
    <% end %>
    <%= link_to "Back", lists_path, class: "btn btn-primary my-3" %>
  </div>
</div>
<div class="footer-banner" style="height: 20vh;"></div>
